1、项目结构
   studyScrapy
     |--dataMonitor
          |--数据监控，主要采取定时更新数据库状态的方式
     |--libMe
          |--工具类集合，是项目通用的
     |--diyicaijing
          |--第一财经
     |--fenghuang
          |--凤凰
     |--hexun
          |--和讯
     |--jiemian
          |--界面
     |--sina
          |--新浪
     |--sohu
          |--搜狐
     |--tengxun
          |--腾讯
     |--wangyi
          |--网易
     |--weixinDetail
          |--微信
     |--weixinSource
          |--微信源

2、启动爬虫
    1）定时爬虫，选择项目目录下的TimerCrawl.py，右键运行即可
    2）单次爬虫，选择项目目录下的launcher.py，右键运行即可

3、爬虫可能存在的问题
    1）如果重启某个项目的时候，log打印只有end输出的且不能正常执行抓取时候可能的原因
       解决：因为项目设置为只有正常启动和抓取完成之后才会设置抓取状态为stop,
       所以在出现异常情况需要重新启动爬虫的时候,需要项目的catchStatus.json中的
       {"status": "stop"}需要设置为stop才能正常抓取
    2）在爬取页面时，有些样式url是以//开头的。当抓取不存在样式的时候需要注意是不是//前没有添加http:
    3）在爬取页面时，有些图片url是以//开头的。当抓取不存在样式的时候需要注意是不是//前没有添加http:
    4）在爬取页面时，微信的图片url是存放在data-src下，需要将data-src下路径获取到，再将data-src替换成src
    5）有些页面的样式，不存在link的标签里面，可能是style的标签里面，所以需要特殊处理style标签，并添加到从link
    标签下载的样式里，有些样式的link标签可能是通过js动态添加的，所以可以分析界面，找到相应的link路径，然后添加到
    代码中的下载路径中
    6）有些页面的样式，采用的是级联的方式，即：在抓取的内容区的外层可能需要手动添加标签进行内容区的包裹，才能正常显示样式
    7）微信容易被禁止的问题，更换ip之后也可能被禁止